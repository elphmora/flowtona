

-r requirements.txt

pytest>=8.3
pytest-asyncio>=0.24   # repository Protocol methods are async (Decision 9)
httpx>=0.27            # FastAPI TestClient / integration tests (Decision 16)

ruff>=0.7
mypy>=1.13              # Protocol conformance enforced via mypy/pyright, not
                        # runtime (Decision 9's refinement)