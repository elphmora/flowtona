

# Runtime dependencies — identity-service

fastapi>=0.115
uvicorn[standard]>=0.32

# Pydantic v2 + email validation (EmailStr, used in models/user.py, models/invitation.py)
pydantic>=2.9
email-validator>=2.2

# Config/settings loading (app/core/config.py)
pydantic-settings>=2.5

# Password hashing — Decision 7 (argon2id)
argon2-cffi>=23.1

# JWT signing/verification — Decision 4 (RS256/ES256 via JWKS)
# [crypto] extra pulls in the cryptography package needed for RS256/ES256
# (not just HS256)
pyjwt[crypto]>=2.9