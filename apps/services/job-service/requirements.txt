# Runtime dependencies -- job-service

fastapi>=0.115
uvicorn[standard]>=0.32

# Pydantic v2 (config/settings loading, app/core/config.py)
pydantic>=2.9
pydantic-settings>=2.5

# Prometheus metrics (app/middleware/metrics.py)
prometheus-client>=0.20

# Outbound HTTP -- ClientServiceClient
httpx>=0.27

# JWT verification via JWKS (Platform Conventions §7)
# [crypto] extra needed for ES256 verification, not just HS256
pyjwt[crypto]>=2.9

# Not yet needed:
#   email-validator>=2.2    -- only if a schema actually uses EmailStr;
#                             contact_email_snapshot may be stored as a
#                             plain str (a frozen snapshot of already-
#                             validated Client Service data, not a
#                             live-validated field) -- decide when
#                             that schema is built.