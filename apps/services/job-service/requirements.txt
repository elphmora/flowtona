# Runtime dependencies -- job-service

fastapi>=0.115
uvicorn[standard]>=0.32

# Pydantic v2 (config/settings loading, app/core/config.py)
pydantic>=2.9
pydantic-settings>=2.5

# Prometheus metrics (app/middleware/metrics.py)
prometheus-client>=0.20

# Outbound HTTP -- ClientServiceClient (app/services/client_service_client.py),
# the platform's first real internal service-to-service call. Moved here
# from dev-only now that it's a genuine runtime dependency, not just a
# TestClient tool.
httpx>=0.27

# Not yet needed -- Phase 1 also adds JWT verification alongside the
# first protected route:
#   pyjwt[crypto]>=2.9      -- JWT verification via JWKS
#   email-validator>=2.2    -- only if a schema actually uses EmailStr